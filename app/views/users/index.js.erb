$('.user-profile-index').append('<%= j render @users %>');
<% if @users.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @users %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
  $('.no-more').delay(1000).fadeIn(1500);
<% end %>